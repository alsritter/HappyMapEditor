/// 定义常量
export const DEFAULT_SIZE = 32; // 格子的默认大小
export const MIN_SIZE = 22; // 格子最小值
export const MAX_SIZE = 50; // 格子最小值
export const CANVAS_WIDTH = 600; // 画布宽度
export const CANVAS_HEIGHT = 600; // 画布高度

export default {
  DEFAULT_SIZE,
  MIN_SIZE,
  MAX_SIZE,
  CANVAS_WIDTH,
  CANVAS_HEIGHT
};
